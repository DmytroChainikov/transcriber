# 🎙️ Audio Transcriber & Analyzer

Автоматична система транскрибації та аналізу телефонних розмов з інтеграцією Google Drive та Google Sheets.

## 📋 Зміст

- [Опис](#опис)
- [Можливості](#можливості)
- [Технології](#технології)
- [Встановлення](#встановлення)
- [Налаштування](#налаштування)
- [Використання](#використання)
- [Структура проекту](#структура-проекту)
- [Принцип роботи](#принцип-роботи)
- [Формат даних](#формат-даних)
- [Troubleshooting](#troubleshooting)

## 📖 Опис

Система автоматично моніторить папку на Google Drive, транскрибує аудіо файли розмов за допомогою Gemini AI, аналізує їх за заданими критеріями та записує результати в Google Sheets з автоматичним збереженням форматування, dropdown списків та умовного форматування.

## ✨ Можливості

### 🎯 Основні функції

- **Автоматичний моніторинг Google Drive** - відстежування нових аудіо файлів у вказаній папці
- **Транскрибація аудіо** - конвертація аудіо розмов у текст через Gemini AI API
- **Інтелектуальний аналіз** - автоматичний аналіз транскрипту за заданими критеріями
- **Запис у Google Sheets** - збереження результатів з повним збереженням форматування
- **Локальне збереження** - дублювання транскриптів на локальний диск
- **Історія обробки** - трекінг оброблених файлів для уникнення дублювання

### 🎨 Робота з форматуванням

- ✅ Збереження dropdown списків (data validation)
- ✅ Збереження умовного форматування (кольори в залежності від значень)
- ✅ Збереження стилів комірок (кольори, шрифти, обрамлення)
- ✅ Автоматичне додавання формул (наприклад, підрахунок балів)
- ✅ Динамічне читання структури полів з таблиці

### 🤖 Аналіз розмов

- Визначення типу звернення
- Аналіз дотримання протоколів обслуговування
- Оцінка якості роботи менеджера
- Перевірка виконання інструкцій
- Автоматичний підрахунок балів

## 🛠️ Технології

### Python Libraries
- **google-generativeai** - Gemini AI для транскрибації та аналізу
- **gspread** - робота з Google Sheets
- **google-api-python-client** - Google Drive API
- **python-dotenv** - управління змінними оточення

### Google Cloud Services
- **Google Drive API** - моніторинг та завантаження файлів
- **Google Sheets API v4** - читання/запис даних, копіювання форматування
- **Gemini AI API** - транскрибація аудіо та аналіз тексту

## 📦 Встановлення

### 1. Клонування репозиторію

```bash
git clone https://github.com/DmytroChainikov/transcriber.git
cd transcriber
```

### 2. Створення віртуального середовища

```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

### 3. Встановлення залежностей

```bash
pip install -r requirements.txt
```

### 4. Налаштування Google Cloud

#### a) Створення Service Account

1. Перейдіть в [Google Cloud Console](https://console.cloud.google.com/)
2. Створіть новий проект або виберіть існуючий
3. Увімкніть API:
   - Google Drive API
   - Google Sheets API
4. Створіть Service Account:
   - IAM & Admin → Service Accounts → Create Service Account
   - Назвіть його (наприклад, "transcriber-service")
   - Надайте роль "Editor" або створіть кастомну роль
5. Створіть JSON ключ:
   - Натисніть на створений Service Account
   - Keys → Add Key → Create new key → JSON
   - Збережіть файл у папку `credentials/`

#### b) Налаштування Google Sheets

1. Створіть Google Таблицю
2. Додайте Service Account email до доступу (Share):
   - Email буде у форматі: `your-service@project-id.iam.gserviceaccount.com`
   - Надайте права "Editor"
3. Структура таблиці:
   - **Рядок 1**: Назва або порожній
   - **Рядок 2**: Заголовки полів
   - **Рядок 3**: Шаблон з dropdown та форматуванням
   - **Рядок 4+**: Дані (заповнюються автоматично)

#### c) Налаштування Google Drive

1. Створіть папку на Google Drive для аудіо файлів
2. Додайте Service Account до доступу:
   - Share → вставте Service Account email
   - Надайте права "Editor"
3. Скопіюйте ID папки з URL:
   - `https://drive.google.com/drive/folders/[FOLDER_ID]`

### 5. Налаштування Gemini API

1. Отримайте API ключ:
   - Перейдіть на [Google AI Studio](https://makersuite.google.com/app/apikey)
   - Створіть новий API ключ
2. Збережіть ключ у `.env` файл

### 6. Конфігурація .env

Створіть файл `.env` на основі `.env.example`:

```bash
cp .env.example .env
```

Заповніть змінні:

```env
# Gemini API
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-1.5-flash

# Google Cloud Service Account
GOOGLE_CREDENTIALS_FILE=credentials/your-service-account-key.json

# Google Drive Configuration
DRIVE_FOLDER_ID=your_drive_folder_id_here

# Google Sheets Configuration
SPREADSHEET_ID=your_spreadsheet_id_here
WORKSHEET_NAME=Sheet1

# Local Storage
LOCAL_TRANSCRIPTS_FOLDER=transcripts

# Monitoring Settings
MONITORING_INTERVAL=60
```

## 🚀 Використання

### Запуск програми

```bash
python main.py
```

Програма автоматично:
1. Обробить існуючі файли в папці Drive (якщо є необроблені)
2. Почне моніторинг папки з інтервалом 60 секунд
3. При появі нового аудіо файлу:
   - Завантажить його
   - Транскрибує
   - Проаналізує
   - Запише результати в Google Sheets
   - Збереже транскрипт локально

### Додаткові опції

#### Показати статистику

```bash
python main.py --show-stats
```

#### Очистити історію оброблених файлів

```bash
python main.py --clear-history
```

#### Видалити конкретний файл з історії

```bash
python main.py --remove-file "filename.mp3"
```

### Зупинка програми

Натисніть `Ctrl+C` для коректної зупинки моніторингу.

## 📁 Структура проекту

```
transcriber/
├── main.py                          # Точка входу, моніторинг Drive
├── transcriber.py                   # Транскрибація та аналіз
├── google_sheets_handler.py         # Робота з Google Sheets API
├── google_drive_handler.py          # Робота з Google Drive API
├── google_drive_file_handler.py     # Обробка файлів з Drive
├── processed_files_tracker.py       # Трекінг оброблених файлів
├── requirements.txt                 # Залежності Python
├── .env                            # Змінні оточення (не в git)
├── .env.example                    # Приклад конфігурації
├── .gitignore                      # Виключення для git
├── credentials/                    # Google Service Account ключі
│   └── service-account-key.json   # JSON ключ (не в git)
├── transcripts/                    # Локальні транскрипти
│   └── *.txt                      # Збережені транскрипти
├── processed_files_drive.json      # Історія обробки
└── transcriber.log                # Логи програми
```

## ⚙️ Принцип роботи

### 1. Моніторинг Drive

```
Google Drive Folder
    ↓
Новий аудіо файл (.mp3, .wav, .m4a)
    ↓
Перевірка в історії обробки
    ↓
Якщо не оброблено → Завантаження
```

### 2. Обробка файлу

```
Аудіо файл
    ↓
Gemini AI (Транскрибація) → Текст розмови
    ↓
Gemini AI (Аналіз) → Структуровані дані
    ↓
Заповнення всіх полів одним запитом
```

### 3. Запис у Google Sheets

```
1. Читання структури з таблиці
   - Рядок 2: Заголовки
   - Рядок 3: Шаблон (dropdown, форматування)

2. Пошук наступного порожнього рядка

3. Копіювання форматування з рядка 3
   - PASTE_FORMAT (стилі, кольори)
   - PASTE_DATA_VALIDATION (dropdown)
   - PASTE_CONDITIONAL_FORMATTING (умовне форматування)

4. Очищення значень в новому рядку

5. Запис даних
   - Основні поля (RAW)
   - Формули (USER_ENTERED)

6. Локальне збереження транскрипту
```

## 📊 Формат даних

### Структура Google Sheets

| Рядок | Призначення | Опис |
|-------|-------------|------|
| 1 | Заголовок | Назва таблиці (опціонально) |
| 2 | Headers | Назви полів |
| 3 | Template | Шаблон з форматуванням та dropdown |
| 4+ | Data | Заповнені дані |

### Приклад полів

```
Рядок 2 (заголовки):
Дата | Тип звернення | Номер телефону | Філія | Менеджер | Оцінка | ...

Рядок 3 (шаблон):
[dropdown: дати] | [dropdown: типи] | [текст] | [dropdown: філії] | [текст] | [формула: =SUM(...)] | ...
```

### Типи полів

1. **Dropdown поля** - вибір зі списку
   - Автоматично визначаються через Data Validation
   - Gemini вибирає значення зі списку
   - Зберігається колір відповідно до умовного форматування

2. **Текстові поля** - вільний текст
   - Короткі описи (1-2 речення)
   - Транскрипти (до 50,000 символів)

3. **Числові поля** - бінарні оцінки (0/1)
   - Використовуються для перевірок
   - Автоматично підраховуються у формулах

4. **Поля з формулами** - автоматичні розрахунки
   - Наприклад: `=F4+G4+H4+I4+J4+K4+M4+O4`
   - Записуються з `USER_ENTERED` для обчислення

## 🔧 Troubleshooting

### Помилки авторизації

**Проблема**: `Error 403: Permission denied`

**Рішення**:
1. Перевірте чи Service Account має доступ до Drive папки та Sheets
2. Переконайтесь що увімкнені потрібні API в Google Cloud Console
3. Перевірте чи правильний шлях до `credentials.json`

### Помилки транскрибації

**Проблема**: `Error: Failed to transcribe audio`

**Рішення**:
1. Перевірте API ключ Gemini
2. Переконайтесь що файл не пошкоджений
3. Перевірте розмір файлу (ліміт ~20MB)
4. Спробуйте інший формат аудіо

### Проблеми з форматуванням

**Проблема**: Форматування не зберігається

**Рішення**:
1. Переконайтесь що рядок 3 має потрібне форматування
2. Перевірте що dropdown створені через Data Validation
3. Переконайтесь що умовне форматування застосоване до всього стовпця

### Дублювання записів

**Проблема**: Один файл обробляється кілька разів

**Рішення**:
1. Видаліть або очистіть `processed_files_drive.json`
2. Перезапустіть програму
3. Файл буде оброблений один раз

### Помилки парсингу відповідей

**Проблема**: Значення містять зайві символи або пробіли

**Рішення**:
- Програма автоматично очищує:
  - Квадратні дужки `[]`
  - Лапки `"` і `'`
  - Зайві пробіли
- Якщо проблема залишається, перевірте промпт в `_analyze_all_fields_at_once()`

## 📝 Логування

Всі дії записуються в `transcriber.log`:

```bash
# Переглянути останні логи
tail -f transcriber.log

# Windows PowerShell
Get-Content transcriber.log -Tail 50 -Wait
```

Рівні логування:
- `INFO` - нормальна робота
- `WARNING` - попередження (не критично)
- `ERROR` - помилки (потребують уваги)

## 🔒 Безпека

- ❌ **НІКОЛИ** не комітьте `.env` та `credentials/` в git
- ✅ Використовуйте `.gitignore` для виключення секретів
- ✅ Обмежуйте доступ Service Account мінімально необхідними правами
- ✅ Регулярно ротуйте API ключі та Service Account ключі
- ✅ Зберігайте credentials в безпечному місці (наприклад, password manager)

## 📄 Ліцензія

Цей проект створений для внутрішнього використання.

## 👨‍💻 Автор

Dmytro Chainikov

## 🤝 Підтримка

При виникненні проблем:
1. Перевірте [Troubleshooting](#troubleshooting)
2. Перегляньте логи в `transcriber.log`
3. Створіть issue в GitHub репозиторії

---

**Версія**: 2.0  
**Остання оновлення**: Жовтень 2025
